Clean Folder
======

This python script takes as argument a path and a limit.
When the limit is reached, the oldest files are deleted.

Example:
clean_folder.py --path "/home/myuser/Downloads" --percent 20